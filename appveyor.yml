os: Visual Studio 2015

install:
  - cmd: refreshenv
  - cmd: set
  - cmd: python --version
  - cmd: wmic OS get OSArchitecture

build: off

build_script:
  - "python coff_test.py"
  - "python lib_test.py"

artifacts:
  - path: tmp
    name: obj
    type: zipåå
  - path: tmp-lib
    name: lib
    type: zip

